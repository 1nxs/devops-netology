#!/usr/bin/env python3
import time
print (time.ctime())

from datetime import datetime
dt = datetime.now()
print(dt)
# выберу метод позже

import socket

service_addr = {
    'drive.google.com': '0',
    'mail.google.com': '0',
    'google.com': '0'
}
